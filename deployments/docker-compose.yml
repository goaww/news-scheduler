version: '3.1'

services:

  scheduler_db:
    image: postgres:12.2
    restart: always
    environment:
      POSTGRES_PASSWORD: example
      POSTGRES_USER: scheduler
      POSTGRES_DB: news_source
    ports:
      - 5432:5432